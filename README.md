# Webpack Template

## Commands
| command       | Description                             |
| ------------- |:---------------------------------------:|
| `build`       | build the project to the `dist` filder. |
| `start`       | start dev server and serve the project. |